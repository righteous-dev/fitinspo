{
  "name": "fitinspo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name fitinspo"
  },
  "devDependencies": {
    "vite": "^8.0.12",
    "wrangler": "^3"
  }
}
